- Fix KCD2's Streamline presentation stalls with application-owned frame-latency synchronization.
- Update the verified NVIDIA Streamline downloader to 2.14.1; keep KCD2 on the installed plugins instead of older driver-cached overrides.
- Incorporate [cmh1448's NR motion-vector fix](https://github.com/cmh1448/OptiScaler_DLSSNR/commit/6446cc8dcc76e2c2b786384300e217f352b2c18b): independent depth/MV offsets and dimensions, `MVLowRes` handling, and per-axis scaling. Adapted for our pre-SR and multipass paths on DX12 and Vulkan.
- Simplify model selection to **NVIDIA (FP8)** and **Experimental (FP8+NVFP4 hybrid)**. FP8 stays the default; the previous hybrid option is removed.
- Add KCD2 setup guidance for SDR, FG ownership, and output FPS caps.

**Upgrade:** extract the complete package, including the updated `nvngx.dll_dlssnr.dll` forwarder. NVIDIA NR/FG runtimes are not bundled; the included helper downloads the official FG files. For uncapped KCD2 FG, disable VSync and set OptiScaler's FPS Limit to **0**. Existing settings are preserved.

**Validation:** x64 builds, motion-metadata regression checks, padded-colour WARP smoke tests, dependency signatures and package checksums passed. KCD2 pacing/DLSS/FG were tested in-game; the newly integrated motion-vector changes still need visual validation in-game.

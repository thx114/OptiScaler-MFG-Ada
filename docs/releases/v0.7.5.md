- Fix NR remaining stuck at startup under DXVK/Proton by advancing its epoch after successful presentations ([#8](https://github.com/wilsjo2/OptiScaler-DLSSNR-PreSR-Multipass/pull/8)).
- Fix flashing in the optional **Generate before SR, apply after SR** mode. Stabilize frame pairing while retaining actual submission waits for NR, private DLSS and residual FG ([#11](https://github.com/wilsjo2/OptiScaler-DLSSNR-PreSR-Multipass/pull/11)).
- Clarify NR control tooltips across passes, preserving the NVIDIA/Experimental model options and unified SR/RR controls ([#13](https://github.com/wilsjo2/OptiScaler-DLSSNR-PreSR-Multipass/pull/13)).

**Upgrade:** install the complete ZIP and preserve your game-specific INI. NVIDIA FP8 remains the default; the promoted hybrid assets are included. NVIDIA NR/FG runtimes remain separate, with the verified Streamline 2.14.1 downloader included.

**Validation:** Release x64 build, deferred-frame sequence regression, NR metadata checks and padded-colour WARP smoke tests passed. No new in-game validation was performed. Repository-wide formatting CI remains a pre-existing failure.

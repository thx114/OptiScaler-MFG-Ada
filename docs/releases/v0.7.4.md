- Shorten NR menu descriptions and clarify what each option changes.
- Use **Apply before Super Resolution** for both SR and RR+SR: checked runs NR before either; unchecked runs it afterward.
- Share model resolution, pass count and profiles between SR and RR. Remove the separate RR controls.
- Keep NVIDIA FP8 as the default and retain the Experimental FP8+NVFP4 hybrid.

**Upgrade:** install the complete ZIP. Legacy `ApplyAfterRR`, `RRPasses` and `RRWorkingScale` settings are ignored; RR now uses your common NR settings.

**Validation:** Release x64 build and existing metadata/padded-colour smoke tests passed. NR before RR remains experimental; visual quality has not been validated. NVIDIA model/FG runtimes are supplied separately as before.

# Open PR integration review, 9 September 2026

Based on main `b7484ef9`, including the unified SR/RR controls.

- [PR #8](https://github.com/wilsjo2/OptiScaler-DLSSNR-PreSR-Multipass/pull/8), head `4a96e741`: accepted. The DXVK branch returns before the native counter update. It now advances the existing counter only for successful real presentations, excluding `DXGI_PRESENT_TEST`. It does not remove NR's feature-creation wait. Bodycam/Proton validation was reported by the author; not reproduced on this Windows host.
- [PR #11](https://github.com/wilsjo2/OptiScaler-DLSSNR-PreSR-Multipass/pull/11), head `c9978c78`: integrated with corrections. A Present between Before and After must not change the pair's logical frame ID. The original helper advanced on both seams and could create a double tick. The corrected clock advances on native Before calls only; bridges keep their submitted counters. Actual submitted epochs remain the authority for private SR, NR and residual-FG feature-creation waits. Sample-and-hold records the consumed pair's epoch. This avoids treating a synthetic logical tick as a submitted GPU command list. DEBUG/TRACE diagnostics remain optional at those log levels.
- [PR #13](https://github.com/wilsjo2/OptiScaler-DLSSNR-PreSR-Multipass/pull/13), head `98e8d48f`: resolved against the newer menu. Keep `Precision=4`, the NVIDIA/Experimental labels, and shared slider tooltips for all passes. Clarify appearance, structure, tone and learned skin selection with concise help. Omit the old helper size, blanket performance claims and promises about the supplied model's handling of values above 1. The [NVIDIA overview](https://research.nvidia.com/labs/adlr/DLSS5/) was accessible; the linked technical PDF returned HTTP 403 during review.

Validation: combined Release x64 build passed; targeted seam-clock regression covers mid-evaluate Present changes, stalled counters, gaps, resets and duplicate bridge evaluations. Existing NR guide-region and padded-colour WARP smoke checks passed. No game benchmarks or additional platform validation were performed.

The repository-wide clang-format check was already failing on main (`b7484ef9`, `cb1f7aa3`, `d2b65cda`) before this integration. The PR checks also report formatting failure. This review does not reformat unrelated source or claim that check passed. Whitespace-error checks passed.
